
language: java
jdk:
  - openjdk11
  - openjdk12
build:
  ci:
    - mvn -B clean test jacoco:report

integrations:                               
  notifications:
    - integrationName: email
      type: email
      on_success: never
      on_failure: never
      on_cancel: never
      on_pull_request: never
